# Building 真实模型运行记录

运行日期：2026-08-18

模型与运行配置：

```text
model: Qwen3.6-35B-A3B-FP8
provider: openai-compatible
controller: building_baseline_react
parallel_tool_calls: false
max_tool_calls: 80
max_output_tokens: 4096
```

本表是单次工程烟雾实验，不代表统计结论。被忽略的原始运行产物位于
`runs/building_qwen_20260818_fixed_v2/`。

| 场景 | 成功 | Tool | Token | Wall time (s) | 关键结果 |
|---|---:|---:|---:|---:|---|
| 跨房间预约 | 是 | 6 | 44,258 | 48.95 | 只完成预约，无提前设备动作 |
| K1324 人数/CO₂ | 是 | 23 | 176,616 | 73.97 | 80.9 分钟计划时间线，结束无设备残留 |
| K1315 正式会议 | 是 | 37 | 421,588 | 133.33 | 打印和全会议设备完成，资源正常释放 |
| K1316 小型研讨 | 是 | 26 | 285,296 | 105.47 | 会中 CO₂ 检查与通风升级完成 |
| K1324 温湿度协调 | 否 | 80 | 1,761,912 | 257.86 | 达到数值目标后继续振荡控制，触发 Tool 上限 |

## 实验直接暴露并已修正的问题

1. 预约场景原先允许模型在会议前一天启动设备。Prompt 已增加任务范围和会议阶段
   约束，验证也会拒绝提前设备动作；修正后 Tool 从 11 降到 6。
2. MQTT 加速发布退出时会丢最后一条消息。Publisher 现在等待 QoS 发布确认，真实
   Mosquitto 回环测试已验证温度、湿度和 CO₂ 三条消息全部到达。
3. 人数场景缺少聚合人数 Tool 和明确时间边界，模型曾推进约 15 小时并留下通风
   设备。已增加 `OccupancyApp.get_room_occupancy()`，明确 6 个阶段并统一使用专用
   通风设备；修正后约 81 分钟结束并通过验证。
4. HVAC 关闭命令仍校验无物理意义的目标温度，导致一次直觉合理的关闭调用被拒绝。
   现在关闭状态会忽略并清除目标温度。

## 尚未解决的主要问题

温湿度协调需要 Controller 层的有限状态/迟滞终止策略，而不是继续增加 Prompt：

```text
precondition -> observe -> within target for N checks -> maintain/stop -> final verify
```

需要限制最大模拟时长、连续无改善次数和重复命令，并把“已经达标但未清理”作为
显式控制状态。下一轮对比实验应在实现该策略后进行，并至少使用多个随机种子。

## 2026-08-25：20 个 L3 的首个 Smoke 基线

运行配置：Qwen3.6-35B-A3B-FP8、vLLM、`building_baseline_react`、串行工具调用。
原始产物位于 `runs/building_qwen36_35b_smoke_20260825/`。

| 场景 | Validation | Tool / LLM | Token | Wall time (s) | 主要失败条件 |
|---|---:|---:|---:|---:|---|
| L3-06 K1324 错峰办公日 | 否 | 49 / 50 | 672,232 | 136.73 | occupied peak CO₂ 2203 ppm，大于 1200 ppm |

本轮验证成功了正常结束、日终空房、设备关闭、无预约残留和无工具运行错误。失败来自
环境质量而非 cleanup：13:01 时模型在 4 人仍占用的情况下关闭通风，随后一次调用
`advance_time(hours=4)` 直接推进到 17:01。14:00 的 14 人返场事件虽被物理 Runtime
处理，但没有中断这次 Agent 等待，模型因此没有重新观测和恢复通风。

同时观察到两个次要问题：模型首次使用了未从 Registry 获得的
`k1324_lighting_01`，被拒绝后能够自行恢复；最终报告把实际执行的空气净化器操作误写
成“加湿器辅助”，说明自然语言总结不能替代 runtime trace 作为事实来源。

在解决长时间推进的事件边界问题前，不启动六场景诊断批次，否则多个长周期场景会
重复暴露同一机制缺陷，无法有效区分会议、功率、打印和多承诺能力。

## 2026-08-25：20 个 L3 全量诊断基线

根据后续要求，在保持同一代码和配置的情况下完成全部 20 场。所有进程均正常结束，
Validation 为 2/20；总计 1,223 次 Tool、1,243 次 LLM 和 20,129,215 Token。原始
产物位于 `runs/building_qwen36_35b_l3_all_20260825/`，逐场结果、模型问题、Scenario
可解性问题和优化顺序见 `l3_live_model_audit_20260825.md`。

## 2026-08-25：第一类问题修复后状态

已修复打印任务隐藏、交互未来信息泄漏与初态污染、首次传感器为空、长时间推进跨越
关键事件、工具参数 Schema 不完整以及 Validator 失败原因过粗等 Scenario/接口问题。
Oracle 与普通 Building 场景均保持连续可重放，完整离线回归为
`162 passed, 1 skipped`。真实模型结果尚未复测，因此上面的 2/20 仍是修复前基线，
不能与新代码混作同一实验结果。

同日完成 L3-06、11、12、16、18、20 修复后诊断复测，结果为 2/6：L3-11 和
L3-18 通过；L3-12 已只剩舒适控制失败；L3-20 的打印任务已通过但仍提前终止。
产物位于 `runs/building_qwen36_35b_l3_diagnostic_after_class1_fix_20260825/`，详细对比
见 `l3_live_model_audit_20260825.md`。这组结果表明第一类可解性问题已被实际模型运行
验证修复，下一轮重心应转到 Controller 阶段状态、终止判据和持续环境控制。

## 2026-08-25：Controller 闭环修复复测

在运行边界、会议 readiness、环境观测、公开承诺排序和功率预算接口加入后，修复目标集
已确认通过 L3-06、12、13、14、16。其中 L3-16 峰值功率为 3857.47 W，4200 W 约束下
越界时长为 0。L3-20 已确认功率、三个会议准备和打印 deadline 通过，但当次完成运行仍
因 K1324 占用期间通风被关闭而发生 CO2 超限。

随后加入时间推进前环境协调守卫，要求有人时先复核环境，并在指标越界且缺少对应控制时
拒绝继续快进。完整离线回归为 `169 passed, 1 skipped`。最终 L3-20 在线复测因远端模型
请求长期无响应而人工终止，没有最终 Validation；保留为网络/服务中断记录，不纳入成功率。
在完成一次不中断的 L3-20 复测及 20 场全量回归前，不更新整体 2/20 基线统计。

### L3-20 环境守卫重试

后续重试已正常获得最终报告，但因达到 180 次 Tool 上限而停止，Validation 未通过；
仿真结束于本地 11:46。环境与功率修复本身有效：peak CO2 714.26 ppm、peak power
3869.96 W、功率越界 0 秒，打印 deadline 通过。环境推进守卫共拒绝 30 次调用，导致
模型在反复协调中耗尽 Tool 预算，未完成后续正式会议、交互和日终清理。

该结果将下一项系统工作明确为“降低 Controller 协调粒度与重复握手成本”，而不是继续
强化 Prompt 或直接提高调用上限。详细分析见 `l3_live_model_audit_20260825.md`。

### L3-20 放宽调用预算验证

保持其余条件不变，将最大调用数从 180 放宽至 320 后，L3-20 完整通过 Validation。
实际使用 162 次 Tool、163 次 LLM、5,846,570 Token，未用满上限；三个会议 readiness、
打印 deadline、环境、功率、预约释放和日终清理全部通过。peak power 4229.42 W，功率
越界 0 秒，occupied peak CO2 649.14 ppm。

这确认当前代码存在正确执行路径，也说明模型轨迹存在方差：前一轮在 180 次内未闭合，
本轮只用 162 次即通过。现阶段可将 320 作为 L3 复杂场景的验证性上限，但正式能力结论
仍应基于同一场景多次重复和全 20 场回归，而不是单次通过。

## 2026-08-28：全量复测后三个失败单元已定向修复

320 次上限的全 20 场复测为 17/20，失败单元为 L3-05、L3-13、L3-20。现已统一空气
质量预警与控制阈值、将打印完成改为 Runtime 定时事件、增加温度越界的 HVAC 强度契约，
并增加不可跨越未就绪会议开始时刻的 readiness 守卫。

定向复测中 L3-05 与 L3-20 分别以 122 和 277 次 Tool 通过；L3-13 最终以 80 次 Tool、
81 次 LLM、2,058,177 Token 通过，打印 deadline、会议准备、环境和日终清理均通过。
最新完整离线回归为 `171 passed, 1 skipped`。

下一步应在完全相同的当前代码版本上重跑全部 20 场，形成可直接比较的最终成功率；在此
之前，结论限定为“原有三个失败单元均已完成定向修复和真实模型验证”，不跨版本拼接为
20/20。

## 2026-08-28：当前版本 20 场最终全量回归

已在完全相同的当前代码版本上完成全部 20 场，结果为 17/20。原始汇总位于
`runs/building_qwen36_35b_l3_all_final_20260828/summary.md`。全批次共使用 2579 次 Tool、
2597 次 LLM、110,388,889 Token，累计墙钟时间 8963.70 秒。

三个未通过场景不能视为同一种失败：L3-10 完成了整日流程，但 K1315 occupied peak
CO2 达到 2466.31 ppm，属于模型环境控制反应过晚；L3-16 与 L3-20 分别在 302、272 次
Tool 后因输入历史达到至少 123905 token，与 4096 output 预算合计超过 128k 上下文而
收到服务端 400。后两者属于 Controller 缺少上下文压缩/滚动窗口的第一类实现问题。

因此当前能力结论为“17 场通过、1 场模型控制失败、2 场因运行框架上下文溢出未完成”。
下一步优先实现阶段摘要、有限原始历史窗口和重复调用抑制，定向重跑 L3-16、L3-20 后再
做全量多次重复；L3-10 则保留为模型持续空气质量控制与结果自检能力测试。
